import 'package:flutter/material.dart';

class ProgramTab extends StatelessWidget {
  const ProgramTab({super.key});
  @override
  Widget build(BuildContext context) => const Center(child: Text("Program"));
}
